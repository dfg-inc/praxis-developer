---
name: resume-work-package
description: Resume Developer work on a claimed or unclaimed Work Package.
---

# Resume Work Package

`praxis doctor --json` then `praxis developer status --wp <WP> --json`. Continue claim → plan → implement → complete. Writes require human confirmation. If CLI cannot run: `LOCAL_RUNTIME_UNAVAILABLE`.
